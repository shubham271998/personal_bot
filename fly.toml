app = "pocket-claude-bot"
primary_region = "sin"

[build]

[env]
  NODE_ENV = "production"
  DB_DIR = "/data"

[mounts]
  source = "bot_data"
  destination = "/data"

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"
